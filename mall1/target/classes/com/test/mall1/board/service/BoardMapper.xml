<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.test.mall1.board.service.BoardMapper">
	<delete id="deleteBoard" parameterType="com.test.mall1.board.service.Board">
		DELETE 
		FROM board 
		WHERE board_no = #{boardNo}
	</delete>
	<update id="updateBoard" parameterType="com.test.mall1.board.service.Board">
		UPDATE board 
		SET member_id = #{memberId}
			,board_title = #{boardTitle}
			,board_content = #{boardContent}
			,board_date = now()
		WHERE board_no = #{boardNo}
	</update>
	<select id="selectBoardView" resultType="com.test.mall1.board.service.Board">
		SELECT 
			board_no as boardNo
			,member_id as memberId
			,board_title as boardTitle
			,board_content as boardContent
			,board_date as boardDate
		FROM board
		WHERE board_no = #{boardNo}
	</select>
	<select id="totalCountBoard"
			resultType="Integer">
		SELECT
			COUNT(*)
		FROM board
	</select>
	<select id="selectBoardList" 
			parameterType="java.util.Map"
			resultType="com.test.mall1.board.service.Board">
		SELECT 
			board_no as boardNo
			,member_id as memberId
			,board_title as boardTitle
			,board_content as boardContent
			,board_date as boardDate
		FROM board
		ORDER BY board_no DESC
		LIMIT #{beginRow}, #{pagePerRow}
	</select>
		<insert id="insertBoard" parameterType="com.test.mall1.board.service.Board">
		INSERT INTO board(
			member_id
			,board_title
			,board_content
			,board_date
			) VALUES(			
				#{memberId}
				,#{boardTitle}
				,#{boardContent}
				,now()
			)
	</insert>
</mapper>